/*INTERATIONS: ALERT, PROMPT, CONFIRM.
alert
 it interact with the user by showing a message and wait for user to press okay 
 alert("click ok");
PROMPT
result=prompt(title,[default]) ;
title- the text to show the visitor
default-optional initial value for input field
let age= prompt("how old are you?",35);
alert(`you are ${age} old`);
CONFIRM
shows message and waits for user to press ok or cancel
Result= confirm(question);
let isBoss =confirm("are the boss?");
alert(isBoss);
all this methods are modal and dont allow the visitor to interact with rest of page til dismissed.

task one
alert("swahili version!!");
let name=prompt("jina lako?", );
alert(`jina langu ${name}.asante!`);
task two
let name= prompt("jina lako?");
let age=prompt("umri wako?");

if (age<18) {
	alert(` Habari ${name}, jaribu tena baada ya miaka michache!`);
}
	else{
		alert(`Welcome! ${name}.Wewe kwa sasa ${age} miaka.`);
		
	}
	*/

alert("mambo");

