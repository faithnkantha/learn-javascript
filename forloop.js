//used when we want to perform activities number of times.
//for loop
//when we clear the number of times we want.
//for(initialization;Condition;incrementation/decrementation)
for( let a=1; a<=10;a++)
{
   document.write("<h1>"+ 10*a+"<h1>" );
}
//checkes for intialization only once.
for( let a=1; a<=10;a+=2)
{
   document.write("<h1>"+ a+"<h1>" );
}